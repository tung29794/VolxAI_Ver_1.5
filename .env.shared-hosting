# VolxAI - Shared Hosting Configuration
# Sao chép file này thành .env trên server
# Copy this file to .env on your shared hosting server

# ========================================
# DATABASE CONFIGURATION
# ========================================
# Típo trên cPanel là "localhost" để kết nối local
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_cpanel_username_volxai_user
DB_PASSWORD=your_mysql_password_here
DB_NAME=your_cpanel_prefix_volxai_db

# ========================================
# SERVER CONFIGURATION
# ========================================
PORT=3000
NODE_ENV=production

# ========================================
# JWT CONFIGURATION
# ========================================
# Giữ bí mật - sử dụng để sign auth tokens
JWT_SECRET=volxai-secret-jwt-key-2024-change-this-in-production

# ========================================
# API ENDPOINTS
# ========================================
# Frontend URL (cPanel sẽ gán port tự động)
# VITE_API_URL=https://yourdomain.com:port/api
# hoặc nếu dùng reverse proxy:
# VITE_API_URL=https://api.yourdomain.com

# ========================================
# APP SETTINGS
# ========================================
PING_MESSAGE=ping pong

# ========================================
# OPTIONAL: BUILDER.IO
# ========================================
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__

# ========================================
# NOTES
# ========================================
# 1. Thay thế tất cả "your_*" bằng giá trị thực từ cPanel
# 2. Giữ bí mật: DB_PASSWORD, JWT_SECRET
# 3. PORT mặc định là 3000, nhưng cPanel có thể gán khác
# 4. NODE_ENV luôn là "production" trên live
